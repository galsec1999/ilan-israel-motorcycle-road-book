# חבילת סקירה — גרסת מסמך 2.3.1

גרסת מוצר: 2.3.0

## 1. מה הושלם

- נוספו 90 מסלולים מאומתים בלי להסיר את 90 המסלולים הקודמים.
- כל מסלול חדש יוצא מאזור המרכז וכולל נקודת הצטרפות משנית בדרך.
- נבנו מסלולי כוכב, לולאות ומסלולי נחש; סיווג לולאה או נחש אושר רק לאחר השוואת גאומטריית היציאה והחזרה.
- מסלולים עם בעיה נשארים בטאב נפרד ומציגים רמת חומרה והערה אחת ברורה. נקודות בעייתיות נשמרות כתוכן ומוחרגות מן הניווט במקום למחוק מסלול שלם.
- כל תכונות 2.2.4 נשמרו: יצוא HTML, שילוב והמשך טיול, הזמנה, WhatsApp, נקודות מפגש, מונה מקומי, מצב כהה, בטיחות, הקראה והעתקה ל־AI.
- נכסי 2.3.0, ה־manifest, עותקי התאימות ומטמון ה־PWA `v2.3.0-build-2` סונכרנו.

## 2. מספרים מדויקים

- 233 רשומות מבוקרות: 180 מסלולים מפורסמים ו־53 מועמדי מחקר מוסתרים.
- 90 PASS ו־90 WARNING; ‏0 reviewing, ‏0 withheld ו־0 failed.
- חומרת WARNING: ‏18 `minor_navigation`, ‏29 `conditional`, ‏43 `major`.
- 824 תחנות: 416 במסלולי PASS ו־408 במסלולי WARNING.
- 23 תחנות מוחרגות מניווט; הן נשארות בסיפור ומציגות סיבה.
- 384 מקורות ייחודיים בכל הקטלוג המפורסם; 0 כשלים.
- בהרחבה החדשה: 49 לולאות, 25 נחשים ו־16 הלוך־וחזור; 74 מסלולים חוזרים בציר אחר.
- בכל אחד משלושת האזורים נוספו 30 מסלולים בדיוק, בחלוקה של 15 PASS ו־15 WARNING.

## 3. קבצים מרכזיים שנוצרו או שונו

- ממשק: `index.html`, ‏`assets/app-v2.3.0.js`, ‏`assets/app-v2.3.0.css` ועותקי התאימות.
- נתונים: `data/config-v2.3.0.js`, ‏`data/release-audit-v2.3.0.js`, ‏`data/route-expansion-v2.3.0.js`.
- PWA: `manifest-2.3.0.webmanifest`, ‏`offline-2.3.0.html`, ‏`sw.js` ועותקי התאימות.
- מחקר ו־QA: שלושה קובצי הרחבה ושלושה דוחות אזוריים תחת `reports/research/`.
- ביקורת: `tools/build-route-expansion.mjs`, ‏`tools/verify-route-geography.mjs`, ‏`tools/audit-release-routes.mjs`, סקריפטי התיקון האזוריים והדוחות תחת `reports/`.
- בדיקות: `tests/validate-v2.mjs`, ‏`tests/browser-qa-v2.3.0.cjs`.
- היסטוריה: `versions/v2.2.4/` נשמרה ולא נדרסה.

## 4. בדיקות ותוצאות

- `node --check` ל־16 קבצים: עבר.
- `node --test tests/validate-v2.mjs`: ‏17/17 עברו.
- `node tools/audit-release-routes.mjs --network --write`: ‏180 מסלולים מפורסמים, ‏0 כשלים סטטיים, ‏0 כשלים במקורות, ‏0 redirect כללי ו־0 soft-404.
- `node tools/verify-route-geography.mjs --network --write`: ‏90/90 מסלולים ניתנים לניתוב, ‏90/90 עוברים שער, ‏0 כשלים; 74/74 לולאות ונחשים עם ציר חזרה שונה.
- צילום `versions/v2.2.4/`: ‏64 קבצים ו־0 הבדלי תוכן מול בסיס 2.2.4, לאחר נרמול סיומות שורה לבדיקת Git.
- צפון: 30/30 OSRM, ‏30/30 Google Maps, ‏102 מקורות, ‏120 נקודות כביש ו־0 חריגות לא מטופלות.
- מרכז ומזרח: 30/30 OSRM, ‏30/30 Google Maps, ‏82 מקורות חיים ו־435 השוואות ייחודיות ללא הפרה.
- דרום: 30/30 OSRM, ‏30/30 Google Maps, ‏80 מקורות חיים ו־29/29 לולאות או נחשים עם חזרה שונה.
- בדיקת הדפדפן שבוצעה לאחר שינויי הממשק עברה ייצוא HTML אמיתי, שילוב, הזמנה, העתקה ללוח, טאב חומרה, החרגות ניווט, 360px, manifest, Service Worker ו־offline ללא שגיאות. ההרצה החוזרת הסופית בדפדפן המובנה נחסמה בהרשאה שמורה ולא נעקפה.

## 5. בעיות, מגבלות וסיכונים

- כבישים, סגירות, מזג אוויר, מצב ביטחוני, שעות ודרישות הזמנה יכולים להשתנות אחרי 09.08.2026; יש לבדוק כל מסלול לפני יציאת קבוצה.
- ארבעה מקורות רשמיים משתמשים בהגנת WAF. הם תועדו ונבדקו בשיטה חלופית, אך ייתכן שלא ייפתחו מכל רשת.
- Google Maps, Waze וקישורים חיצוניים דורשים אינטרנט; מעטפת האתר זמינה offline לאחר טעינה ראשונה.
- האתר אינו מוגן בסיסמה. `robots.txt` ו־`noindex` מפחיתים גילוי בלבד; כל מי שמקבל קישור חי יכול לפתוח ולהעביר אותו.
- האתר החי ציבורי ואינו מוגן בסיסמה; המשתמש אישר במפורש את פרסום 2.3.0 ל־GitHub Pages.

## 6. מה נשאר

- סקירת משתמש מדגמית באתר החי לאחר רענון מטמון ה־PWA.
- כל הרחבה עתידית תיפתח בסבב חדש ובגרסה חדשה; אין עבודה פתוחה בתוך שחרור 2.3.0.

## 7. שער האיכות

שערי הנתונים, הגאוגרפיה, המקורות, הקוד וה־PWA המקומיים עברו, והמשתמש אישר את שער הפרסום החיצוני.

## 8. המלצה לשלב הבא

להשתמש באתר החי בכתובת `https://galsec1999.github.io/ilan-israel-motorcycle-road-book/` ולפתוח סבב גרסה חדש רק עבור שינוי נוסף שאושר.
