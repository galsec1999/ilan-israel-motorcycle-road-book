# הפעולה הבאה — גרסת מסמך 2.4.0

גרסת מוצר: 2.4.0

חבילת השימושיות של 2.4.0 מוכנה לבדיקת שחרור מקומית. קטלוג 2.3.0 נשאר ללא שינוי: 180 מסלולים מפורסמים, מהם 90 PASS ו־90 עם הערה; 53 מועמדי מחקר אינם מפורסמים.

סטטוס QA ופרסום: **pending**.

הפעולות הבאות, לפי הסדר:

1. להריץ בדיקות תחביר ואת `tests/validate-v2.mjs`.
2. להריץ את `tests/browser-qa-v2.4.0.cjs` ולאמת במיוחד 360px ללא קטיעת טקסט.
3. לבדוק ידנית את שמונת השיפורים: מסננים ומיונים, כרטיסים, קיצורי בחירה, deep links, מצב אישי מקומי, מפה מוגדלת, „בחר לי טיול” ו„מוכן להפצה”.
4. לאמת שאומדן legacy מסומן כאומדן ושאין הצגה שלו כמרחק מדוד.
5. לאמת manifest, ‏Service Worker, offline, התקנת PWA ושם מטמון עם prefix `ilan-roadbook-live-`.
6. לאמת ש־`versions/v2.3.0/` נשמר ושעותקי התאימות מסונכרנים לנכסי 2.4.0.
7. רק לאחר מעבר השער: לבצע commit ו־push לענף 2.4.0, לקדם ל־`main` ולפרוס ב־GitHub Pages.
8. לאחר הפריסה: לבדוק באתר החי גרסה 2.4.0, טעינת 180 המסלולים, קישורים ישירים, manifest, ‏Service Worker, התקנה, offline, ‏`robots.txt` ו־`noindex`.

המשתמש כבר אישר את ה־commit וה־push לאחר מעבר QA. אין צורך לבקש אישור נוסף עבור פרסום אותה חבילת 2.4.0, אך אין לפרסם אם שער האיכות נכשל.
